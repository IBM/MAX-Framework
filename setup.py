from setuptools import setup

setup(name='maxfw',
      version='0.1',
      description='',
      url='',
      author='CODAIT',
      author_email='',
      license='Apache',
      packages=['maxfw', 'maxfw.core', 'maxfw.model'],
      zip_safe=True)
